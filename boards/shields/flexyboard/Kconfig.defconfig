if SHIELD_FLEXYBOARD_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "FLEXYBOARD"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_FLEXYBOARD_LEFT || SHIELD_FLEXYBOARD_RIGHT

config ZMK_SPLIT
    default y

endif